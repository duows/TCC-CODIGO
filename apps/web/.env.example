# URL base da API consumida pelo cliente. Embutida no bundle em build-time
# (convenção NEXT_PUBLIC_*), portanto precisa estar definida no ambiente de
# build da plataforma de deploy, não apenas em runtime.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
